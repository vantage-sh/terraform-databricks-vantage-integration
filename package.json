{
  "name": "terraform-databricks-vantage-integration",
  "version": "1.0.0",
  "private": true,
  "description": "Terraform module for Databricks Vantage integration",
  "scripts": {
    "release": "semantic-release"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "semantic-release": "^25.0.0"
  },
  "release": {
    "branches": [
      "main"
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/github"
    ]
  }
}
